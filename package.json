{
  "name": "@webfaaslabs/mathsumasync",
  "version": "0.0.1",
  "description": "sum x + y",
  "faas":{
    "category": "labs",
    "public": "true"
  },
  "main": "index.js",
  "scripts": {
    "test": "node dist/test/sum",
    "clean": "rm -rf dist",
    "start": "npm run clean && tsc -w",
    "build": "tsc && cp package.json dist && cp .npmignore dist",
    "publisher": "cd dist && npm publisher --access public",
    "pack": "cd dist && npm pack",
    "buildAndPublisher": "npm run build && npm run publisher"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@webfaaslabs/mathsum": "0.0.1"
  }
}
